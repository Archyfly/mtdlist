<nav class="navbar navbar-light" style="background-color: #999999;">
  <div class="container">
    <div class="navbar-header">
      <% if user_signed_in? && current_user.present? %>
          <%= link_to 'My ToDo list', user_projects_path(current_user), class: 'navbar-brand' %>
          <%= link_to 'Create new project', new_user_project_path(current_user), class: 'navbar-brand' %>
      <% else %>
        <%= link_to 'About', root_path, class: 'navbar-brand' %>
      <% end %>
    </div>
    <div id="navbar">
      <ul class="navbar-btn navbar-link">
          <% if user_signed_in? && current_user.present? %>
            <%= link_to "#{current_user.email}", user_path(current_user) %>
            <%= link_to 'Log out', destroy_user_session_path, method: :delete %>
          <% else %>
            <li><%= link_to 'Log In', new_user_session_path %></li>
            <li><%= link_to 'Sign Up', new_user_registration_path %></li>
          <% end %>
        </ul>
      </ul>

    </div>
  </div>
</nav>
